Q.2-1のテーブルに対してp.43のセレクト文を実行した
ときの結果を理由とともに予想する。

A.
結果：
sum_loadカラムにserverごとの現在行までの合計値が
表示される

理由：
・PARTITION BYによって集合されるが、行がひとつに
まとまらない
・集計する範囲が行ごとではなく、PARTITION BY
によって集合した範囲になるので、serverごとの合計が
算出される